## Flight Blender local environment (example)
##
## Copy to `deployment_support/.env.local` and replace all `change-me-*` values.
## Never commit real secrets (SECRET_KEY, passwords, client secrets).

# Django
SECRET_KEY=change-me-secret-key
IS_DEBUG=1

# Auth (dev-only convenience)
PASSPORT_AUDIENCE=testflight.flightblender.com
PASSPORT_JWKS_URL=http://local.test:9000/.well-known/jwks.json
BYPASS_AUTH_TOKEN_VERIFICATION=1
DISABLE_JSON_LOGGING=1

# Optional feature flags
ENABLE_CONFORMANCE_MONITORING=1
USSP_NETWORK_ENABLED=0

# Redis
REDIS_HOST=redis
REDIS_PORT=6379
REDIS_PASSWORD=change-me-redis-password
REDIS_BROKER_URL=redis://:change-me-redis-password@redis:6379
HEARTBEAT_RATE_SECS=2

# Optional external UI
FLIGHT_SPOTLIGHT_URL=http://local.test:5000

# Deployment URL
FLIGHTBLENDER_FQDN=http://host.docker.internal:8000

# Database (choose one)
USE_LOCAL_SQLITE_DATABASE=1
# DATABASE_URL=psql://mydatabaseuser:mypassword@myproject_db:5432/myproject_db

# Postgres Docker (if using Postgres)
POSTGRES_USER=mydatabaseuser
POSTGRES_PASSWORD=change-me-postgres-password
POSTGRES_DB=mydatabase
POSTGRES_HOST=myproject_db
PGDATA=/var/lib/postgresql/data/pgdata
